/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "agentvanzariaistorage2.blob.core.windows.net",
    ],
  },
};

export default nextConfig;
